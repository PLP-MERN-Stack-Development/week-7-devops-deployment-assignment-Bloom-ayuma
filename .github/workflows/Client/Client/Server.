✅ Client .env.example
// 📄 client/.env.example
// ------------------------------
VITE_API_BASE_URL=https://your-backend-api.com/api

// ------------------------------
// ✅ Server .env.example
// 📄 server/.env.example
// ------------------------------
PORT=5000
MONGO_URI=mongodb+srv://yourUser:yourPass@cluster.mongodb.net/bug-tracker
NODE_ENV=production

// ------------------------------
// ✅ Production Preparation Scripts
// 📄 server/package.json
// ------------------------------
"scripts": {
  "start": "node server.js",
  "dev": "nodemon server.js",
  "test": "echo \"No tests specified\" && exit 0"
},

// 📄 client/package.json
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview"
},

// ------------------------------
// ✅ Logging & Monitoring Suggestions
// ------------------------------
// - Use morgan (for logging HTTP requests)
// - Use Sentry (frontend & backend error tracking)
// - Use uptime monitoring tools like UptimeRobot or Pingdom

// ------------------------------
// ✅ Deployment Platforms
// ------------------------------
// - Backend: Render (connect GitHub, set MONGO_URI, and deploy with "npm start")
// - Frontend: Vercel (auto-detect React, set VITE_API_BASE_URL in UI)
